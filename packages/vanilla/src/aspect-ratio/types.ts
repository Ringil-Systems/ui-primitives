export interface AspectRatioOptions {
  ratio?: number;
}

export interface AspectRatioElements {
  root: HTMLElement;
  wrapper: HTMLElement;
  content: HTMLElement;
}

export interface AspectRatioEvents {
  // No specific events for this component
}
